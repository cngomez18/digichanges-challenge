{
  "compilerOptions": {
    "target": "ES6",                                  /* Versión de JavaScript a la que se compilará el código */
    "module": "commonjs",                            /* Sistema de módulos a utilizar */
    "strict": true,                                  /* Activa todas las comprobaciones estrictas de tipo */
    "esModuleInterop": true,                         /* Habilita interoperabilidad entre módulos ES6 y CommonJS */
    "skipLibCheck": true,                            /* Omite la verificación de tipos en los archivos de definición de tipo */
    "forceConsistentCasingInFileNames": true,        /* Asegura que el caso en los nombres de archivos sea consistente */
    "baseUrl": "./src",                              /* La base del directorio para resolver módulos */
    "paths": {
      "*": ["node_modules/*"]                        /* Mapea todos los módulos a node_modules */
    },
    "outDir": "./dist",                              /* Directorio de salida para los archivos compilados */
    "rootDir": "./src"                               /* Directorio raíz de entrada para los archivos de TypeScript */
  },
  "include": ["src/**/*.ts"],                        /* Archivos de entrada para la compilación */
  "exclude": ["node_modules"]                        /* Archivos y directorios a excluir de la compilación */
}
